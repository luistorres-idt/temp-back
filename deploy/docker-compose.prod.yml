version: '3.9'

services:

  nginx:
    image: nginx:alpine
    container_name: temp-nginx
    restart: unless-stopped
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf:ro
      - ../../front/dist:/usr/share/nginx/html:ro
      # Descomenta cuando configures SSL con certbot:
      #- ./certbot/conf:/etc/letsencrypt
      #- ./certbot/www:/var/www/certbot
    networks:
      - temp-net
    depends_on:
      backend-api:
        condition: service_healthy

  backend-api:
    build:
      context: ..
      dockerfile: Dockerfile
    container_name: temp-backend
    restart: unless-stopped
    expose:
      - "3000"
    env_file:
      - ../.env
    depends_on:
      - redis
    healthcheck:
      # wget porque node:22-alpine no incluye curl
      # /healthy es el endpoint real definido en app.ts
      test: ["CMD", "wget", "--quiet", "--tries=1", "--spider", "http://localhost:3000/healthy"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 60s
    networks:
      - temp-net

  redis:
    image: redis:7-alpine
    container_name: temp-redis
    restart: unless-stopped
    expose:
      - "6379"
    networks:
      - temp-net

networks:
  temp-net:
    driver: bridge
